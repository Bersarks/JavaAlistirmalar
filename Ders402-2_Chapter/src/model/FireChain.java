package model;

public class FireChain extends Villain{

	public FireChain(String name, int damage, boolean isKillable, int health, MoveTypeEnum moveType, Coordinate coordinate) {
		super(name, damage, isKillable, health, moveType, coordinate);
	}
}
