package model;

public class FireChain extends Villain{

	public FireChain(String name, boolean isKillable, int health, MoveTypeEnum moveType, Coordinate coordinate) {
		super(name, isKillable, health, moveType, coordinate);
	}
}
